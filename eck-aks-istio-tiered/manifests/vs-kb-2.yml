---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: kb-vs-2
  namespace: cluster-2
spec:
  hosts:
  - "kb-2.${domain}"
  gateways:
  - gw-2
  http:
  - match:
    - uri:
        prefix: "/"
    route:
    - destination:
        host: kb-2-kb-http.cluster-2.svc.cluster.local
        port:
          number: 5601